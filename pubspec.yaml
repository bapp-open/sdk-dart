name: bapp_api_client
description: A Dart client for the BAPP platform API. Supports CRUD, pagination, tasks, file uploads, and long-running async operations.
version: 0.3.0
homepage: https://www.bapp.ro
repository: https://github.com/bapp-open/sdk-dart
topics:
  - api
  - client

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  http: ^1.1.0
